<UserControl x:Class="CybersecurityChatbot.ChatbotSentinel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Width="800" Height="1000">
    <Grid  Background="Black">
        <Border BorderThickness="3" Margin="10" CornerRadius="20">
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FF00F2FF" Offset="0"/>
                    <GradientStop Color="#FFFF00D4" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>

            <Grid Margin="30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="120"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,20,0,10">
                    <TextBlock Text="SECURESA CHATBOT" FontSize="52" FontStyle="Italic" FontWeight="ExtraBold" HorizontalAlignment="Center">
                        <TextBlock.Foreground>
                            <LinearGradientBrush>
                                <GradientStop Color="#FFFF0055" Offset="0"/>
                                <GradientStop Color="#FF00F2FF" Offset="1"/>
                            </LinearGradientBrush>
                        </TextBlock.Foreground>
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="20" Color="#FF00F2FF" ShadowDepth="0"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </StackPanel>

                <Grid Grid.Row="1" Margin="0,10">
                    <Separator Background="#FFFF00D4" VerticalAlignment="Center" Height="2"/>
                    <Border Background="Black" BorderBrush="#FF00F2FF" BorderThickness="2" Width="40" Height="45" CornerRadius="5" HorizontalAlignment="Center">
                        <TextBlock Text="🔒" Foreground="#FF00F2FF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>

                <ListBox x:Name="lstChatHistory" Grid.Row="2" Background="Transparent" BorderThickness="0" Margin="0,20" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>

                <Grid Grid.Row="3" VerticalAlignment="Bottom">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="200"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" BorderBrush="#FF00F2FF" BorderThickness="2" CornerRadius="10" Height="60" Margin="0,0,20,0">
                        <TextBox x:Name="txtUserInput" Background="Transparent" Foreground="White" BorderThickness="0" 
                                 FontSize="22" VerticalContentAlignment="Center" Padding="15,0" CaretBrush="#FF00F2FF"/>
                    </Border>

                    <Button x:Name="btnSend" Grid.Column="1" Content="ENTER" Click="btnSend_Click" Height="60" Width="180"
                            Foreground="White" FontSize="20" FontWeight="Bold" Background="Transparent">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border" BorderBrush="#FFCCFF00" BorderThickness="3" CornerRadius="15" Background="#22CCFF00">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="15" Color="#FFCCFF00" ShadowDepth="0"/>
                        </Button.Effect>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>